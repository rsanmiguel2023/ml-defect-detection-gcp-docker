# Models Folder

Do not commit trained model files to GitHub.

Expected local artifacts in later stages:

```text
models/tensorflow_effnet_defect.keras
models/pytorch_resnet18_defect.pt
```

Final model artifacts should also be uploaded to Google Cloud Storage.
